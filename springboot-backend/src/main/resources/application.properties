# Application Info
spring.application.name=ebike-system
server.port=8080

# Database Settings (H2 In-Memory for instant local execution)
spring.datasource.url=jdbc:h2:mem:ebikedb;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=rooot
spring.datasource.password=Fa$cimated4
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# JPA & DDL Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Groq / AI Chat Configurations
groq.api.key=${GROQ_API_KEY:gsk_mock_api_key_for_testing}

# H2 Console (enable for easy database debugging)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=true
